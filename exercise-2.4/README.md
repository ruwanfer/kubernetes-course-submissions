# Exercise 2.4: Project Namespace

Created namespace "project" and moved all project components:
- todo-backend (deployment + service)
- todo-app/frontend (deployment + service)

All YAML files include: `namespace: project` in metadata
