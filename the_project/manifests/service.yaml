apiVersion: v1
kind: Service
metadata:
  name: todo-service
spec:
  type: NodePort
  selector:
    app: todo
  ports:
    - name: http
      nodePort: 30081
      protocol: TCP
      port: 80
      targetPort: 3000
