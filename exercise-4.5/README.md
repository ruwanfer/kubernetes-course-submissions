# Exercise 4.5: The Project - Add "Done" Feature

## Updates Made:

### 1. Todo Backend:
- Added `done: false` field to todos
- Added PUT endpoint: `PUT /todos/:id`
- Can mark todos as done: `{ "done": true }`

### 2. API Endpoints:
- `PUT /todos/:id` - Mark todo as done
- Todos now have `done` field (true/false)
