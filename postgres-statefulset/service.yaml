apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: exercises
spec:
  ports:
  - port: 5432
    name: postgres
  clusterIP: None  # Headless service
  selector:
    app: postgres
